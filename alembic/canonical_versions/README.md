Canonical Alembic baseline.

This directory is the active migration chain.

The historical chain remains frozen in `alembic/versions/` as legacy
archive material and is no longer used by Alembic command execution.
