fix(dashboard): reparar queries academy — legacy tables → real tables

- courses → academy_courses
- enrollments → academy_enrollments
- status = 'active' → status = 'ACTIVO'
- certificate_issued → approved
