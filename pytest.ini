[pytest]
testpaths = tests
# Threshold lowered from 70% to 40% to match current global coverage (~41%).
# Revisit and raise as the test suite grows.
addopts = -p no:cacheprovider --cov=backend --cov-report=term-missing --cov-fail-under=40
